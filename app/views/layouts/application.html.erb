<!DOCTYPE html>
<html>
<head>
  <title>Metly |  <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
  <%= render 'layouts/openid' %>

</head>
<body>
<p class="notice" style="display: none;"><%= notice %></p>
<p class="alert" style="display: none;"><%= alert %></p>
<%= yield %>

<%=
    #TODO remove this
    debug(params) if Rails.env.development?
%>
<div class="modal" id="feedback" tabindex="-1" role="dialog" aria-labelledby="feedbackLabel" aria-hidden="true" style="display: none;">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <p>You can provide feedback/feature request/problems to us.</p>
  </div>
  <div class="modal-body">
    <textarea style="width: 100%; position: relative; right: 7px;"></textarea>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
    <button class="btn send btn-primary" aria-hidden="true">Send</button>
  </div>
</div>
</body>
</html>
